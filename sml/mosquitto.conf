# Mosquitto MQTT broker configuration for TelAF Simulation
listener 1883
listener 0 /tmp/simula-mqtt.sock
allow_anonymous true
log_dest stdout

# Persist retained messages across broker restarts so subsystem-ready
# events survive a supervisord restart without waiting for MPSS to reconnect.
persistence true
persistence_location /tmp/mosquitto_persist/

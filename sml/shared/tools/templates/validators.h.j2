// Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
// SPDX-License-Identifier: BSD-3-Clause-Clear
//
// AUTO-GENERATED by sml/simula_shared/tools/gen_cpp.py. DO NOT EDIT.
// Source: sml/simula_shared/schemas/**/*.json
//
// NOTE: no JSON-Schema-constraint-checking library (e.g. valijson) is
// linked into the PA build, so this header only embeds each schema as a
// lookup-by-topic-id nlohmann::json constant -- it does not perform
// jsonschema-style validation the way generated/python/validators.py does.
// PA relies on its own wireToXxx/xxxToWire functions for field-level
// decoding; this table exists so a future C++ validator pass has the
// schemas available without re-reading the JSON files at runtime.
#pragma once

#include <nlohmann/json.hpp>
#include <string>
#include <unordered_map>

namespace topics::schemas {

inline const std::unordered_map<std::string, nlohmann::json>& all()
{
    static const std::unordered_map<std::string, nlohmann::json> kSchemas = {
{%- for m in messages %}
        { "{{ m.topic_id }}", nlohmann::json::parse(R"JSON(
{{ m.schema | tojson(indent=4) }}
)JSON") },
{%- endfor %}
    };
    return kSchemas;
}

inline const nlohmann::json& schema(const std::string& topic_id)
{
    return all().at(topic_id);
}

}  // namespace topics::schemas
